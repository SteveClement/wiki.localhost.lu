# A cuda enable WEP/WPA Cracker called pyrit

```
 sudo apt-get install libpcap-dev python-dev
 svn co http://pyrit.googlecode.com/svn/trunk/ pyrit
 cd pyrit/pyrit
 python setup.py build
 sudo python setup.py install
```
